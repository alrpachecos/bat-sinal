import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  inputText: {
    width: '100%',
    color: '#000000',
    fontSize: 20,
    borderWidth: 1,
    borderRadius: 5,
    padding: 10,
    marginBottom: 30,
  },
});
